package com.example.hushley.Screens

import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.navigation.NavHostController

@Composable
fun Dog1Screen(navController: NavHostController) {
    Text(text = "Dog Info for Dog 1", modifier = Modifier.fillMaxSize())
}

@Composable
fun Dog2Screen(navController: NavHostController) {
    Text(text = "Dog Info for Dog 2", modifier = Modifier.fillMaxSize())
}

@Composable
fun Dog3Screen(navController: NavHostController) {
    Text(text = "Dog Info for Dog 3", modifier = Modifier.fillMaxSize())
}

@Composable
fun Dog4Screen(navController: NavHostController) {
    Text(text = "Dog Info for Dog 4", modifier = Modifier.fillMaxSize())
}

@Composable
fun Dog5Screen(navController: NavHostController) {
    Text(text = "Dog Info for Dog 5", modifier = Modifier.fillMaxSize())
}

@Composable
fun Dog6Screen(navController: NavHostController) {
    Text(text = "Dog Info for Dog 6", modifier = Modifier.fillMaxSize())
}